# 收藏原画 v2：猫猫和其余狗狗

2026-09-08，Poe GPT-Image-2，high，1024x1024，逐张生成44张：猫猫0–23、狗狗24–42和47。
狗狗43–46沿用已采用的 dog-epic-atlas.webp，本次没有重新生成或修改映射。

角色毛色、服饰、道具根据原图逐张核对；故事、名字、职业、稀有度、分组、编号全部保持。
每张参考对应原图切片及已批准的北风新图风格。最终提示词见 prompts.json。
PNG原稿和被弃用变体保留于本工作区 artifacts/collection-art-v2/，不进入App包体。

## 程序资源

从1024方形原稿用Lanczos等比例缩到512，按编号由程序逐格拼接，用WebP quality=88、method=6编码。

| 资源 | 像素尺寸 | 映射 | 字节 |
| --- | --- | --- | --- |
| cat-atlas-v2.webp | 2048×3072 | 0–23，每行4张 | 2133162 |
| dog-atlas-v2.webp | 2048×2560 | 24–42、47，每行4张 | 1879572 |

旧图集保留作来源参考，但已从Flutter资源清单移除；App只打包新图集与既有狗狗史诗图集。
每张原画单独生成，图集只由程序拼接；对应行边界均相差512，避免非等比缩放。

## 原画复核

44张已逐张检查：主体和道具可辨，无拉伸，毛色、服饰和职业对应正确。
普通卡以职业日常为主；稀有卡增加事件场景；史诗和传说按原故事保留各自奇幻细节。
小麦的法棍、信仔的信封、木木的木飞机、琥珀的两瓶药水、北辰的星图、
金栗的面食、眠眠的发光画笔、星弥的愿望瓶均已核对；狗狗按原犬种与关键道具检查。

斑斑和蓝调最初变体误画了人类背景角色，已单独重新生成并复核为动物观众/邻居。
其他卡无需重画。网络连接中断的11项已补齐，其中初始中断不代表有效图片生成。
本次余额从578357降至220181，差额358176点，包含44张采用图及2张弃用变体。

## 需求追踪复核（Core Contract）

| 需求 | 实现及证据 | 结果 |
| --- | --- | --- |
| 替换所有剩余原画 | 44张独立图，目录默认猫猫资源和20处狗狗映射、新pubspec资产清单 | 通过 |
| 保留已批准的4张狗狗史诗 | 生成前后catalog导出逐字段比较43–46完全一致 | 通过 |
| 角色与收藏不变 | 48项index/groupId/job/name/rarity/story/clue逐字段比较一致 | 通过 |
| 正方形且无邻格泄漏 | 实际资源解码尺寸测试，48个切片等宽高且唯一；既有三种视口裁切测试 | 通过 |
| 图鉴筛选与详情正确 | 明暗狗狗筛选交互测试 | 通过 |

新增尺寸测试先在旧图上失败（猫猫0原切片256×304），更换图集后通过。
MUST 无代码级阻塞；SHOULD 无新增偏离；MAY 沿用既有4列裁图组件。

## 验证记录

- 聚焦测试10项通过；完整 `bash tool/verify.sh` 通过7个检查，汇总17条警告。
- Android emulator-5560，默认1440×3120、density 560：实际运行生产
  ScratchPage、ScratchCollectibleCard、ScratchCardDetail，捕获并逐张检查48个详情和14个图鉴状态。
  原画比例、角色映射、邻格隔离、稀有度标签均正确，没有发现新增视觉问题。
- 检查入口和截图脚本仅存于忽略的 artifacts/collection-art-v2/；使用隔离的内存收藏数据，
  通过生产控件回调切换状态。此证据覆盖Android实际渲染，不代表联网抽卡或双设备同步验收。
- 浅色360×640逻辑视口（1080×1920、density 480）补查12个详情、10个图鉴状态，
  图像保持正方形，文字和卡片布局正常。窄屏筛选条沿用横向滚动，长故事沿用纵向滚动。
- 检查期间Flutter日志没有资产加载异常或布局溢出。最终UX结论：通过，无新增阻塞项。
- 验收后恢复原始显示尺寸和密度，移除检查端口转发，安装并启动含新资源的普通入口APK，
  保留原有应用数据，并释放Android独占租约。
