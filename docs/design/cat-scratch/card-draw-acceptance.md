# 抽卡 UX 验收

范围与 Profile：Core Contract；连续单抽交互保留。2026-09-08 按用户纠正恢复原有 20% 中奖、80% 落空；此前每抽必得是实现误解，不能作为批准基线。

MUST：原有中奖及条件稀有度概率、落空不发奖、四档层次、有限预接、无十连、可靠保存和旧存档迁移。
SHOULD：公共控件沿用 token，奖品与操作可读；无新增偏离。
MAY：六张最近获得、中奖 click 与分级触感；落空只翻面和淡出。

## 需求追踪复核

| 需求/受影响状态 | 实现与自动化证据 | 复核结果 |
| --- | --- | --- |
| 20% 中奖、80% 落空；中奖后四档比例不变 | Controller._drawTicket / drawScratchCollectible；中奖门槛和稀有度边界测试 | 仅中奖更改数量和首次日期 |
| 落空展示与继续 | CardDrawStage._miss；窄屏页面测试 | 显示「空白卡」，无 NEW、稀有度或中奖演出；同一按钮继续 |
| 最近获得、详情与收卡 | Flow._advance 和页面中奖判断；落空接中奖 flow test | 空抽不进入最近获得，不打开候选藏品详情；落空淡出 |
| 保存失败与重试 | 不可变回执及串行持久化；中奖/落空失败测试 | 不重抽、不重复发奖，失败停止预接 |
| 初始、恢复、迁移 | v3 保存 winning；v1 三种旧票迁移与 v2 pending/claimed 测试 | 原中奖/空票不改结果；v2 已得收藏保留，待抽只确定一次中奖标记 |
| 单抽、有限预接、离开/后台 | 原 flow 状态机与现有停止/并发测试 | 没有十连或无人操作的持续抽取 |
| 中奖四档、明暗两尺寸 | 四档页面及现有 phone 矩阵 | 中奖原动画保留，默认文案去掉每抽必得 |

完整规格、生产源码和 diff 的独立复核通过，无阻塞发现。测试覆盖新空抽路径和旧中奖路径。固定 runner 未增加截图。

## 自动化与运行时

收藏特性 68 项测试通过；`bash tool/verify.sh` 通过 7 项检查，17 条依赖更新/Kotlin 迁移提示。此后工作区另一项详情页开发曾使定向包构建暂时失败；本次未改动那部分文件，详情页修正后 Android 定向包构建通过。

Android API 36 实际生产 ScratchPage/Controller/Flow，使用独立存储和可控随机源。检查深色 412×891、浅色 360×640 的落空、继续中奖、最近获得、落空保存失败和重试；落空没有增加收藏，重试保留同一结果，只有中奖进入最近获得。已亲自检查截图，卡面、状态、按钮无溢出或遮挡。截图为临时输入；按用户要求单独分享落空截图。设备安装空间不足通过保留数据并替换程序解决，验收后恢复原设备程序和尺寸，释放租约。

## Verdict

complete：本次概率修复的需求复核、测试构建、实际 Android 状态验证和截图检查通过。无未解决 MUST/SHOULD 项。


空白卡外观：素纸、雾纹、几何压纹、珠光、星箔五款中性纸面；按抽取序号独立确定外观，权重 55/25/14/5/1，重建不跳变，不消耗奖品随机源。样式不持久化为收藏、无数量、无图鉴或进度，中奖概率保持 20%。少见材质翻面时有短扫光，无稀有度徽章或中奖音。

本次空白材质复核：MUST 中性外观、五种有频率差异的样式、无收藏数量、概率不变均由 BlankDrawCard 和未修改的 controller/flow 满足；页面测试改为断言「空白卡」，完整 verify 通过 7 项检查（17 条既有工具链提示）。Android 实际生产页面逐一检查五款深色 412×891、浅色 360×640 截图，确认文字/按钮可读且无溢出。定向运行所有结果 total/recent 均为零。无新增 SHOULD 偏离；MAY 为少见材质的有限扫光。Verdict: complete。
