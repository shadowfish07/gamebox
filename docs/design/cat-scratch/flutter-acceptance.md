# Flutter 手机 MVP 验收

日期：2026-09-07。旧 HTML 原型不作为本次移动端验收依据。

## 自动化

- `bash tool/verify.sh` 通过（7 个顶层检查，17 条汇总警告），覆盖设计 tokens、Dart 静态分析、Flutter 测试、Go/Godot 回归、Android 单元测试、实际 debug APK 构建及打包检查。
- 刮奖专项包含四档概率边界、单票幂等领取、重复数、职业证顺序、四区域完成、唯一覆盖率、损坏/失败存档，以及上一笔保存中下一笔完成时仍领取一次。
- Flutter 组件运行覆盖 360×640、412×891 明暗界面；验证三种刮法的票面与底部操作位置、详情、展柜，以及真实 Pointer 手势和切页保留轨迹。
- 大厅新增入口后，原有列表间距与大字号测试按新增卡片及真实滚动更新。

## Android 运行检查

实际安装本次构建 APK，在工作区持有的 slot 0 / `emulator-5560`（API 36）上检查。已通过实际手势验证拍立得入册、职业证先线索后照片、四区域逐个揭晓及全部刮开。随机实测得到渔夫一次、花艺师两次，进度为 2/24、总刮开 3 张，重复猫没有增加图鉴种类。

Android 系统 Back 返回登录页，再进入后保留当前票。强制停止应用并重新启动后，仍保留 NO.004 职业证、部分刮擦轨迹、2/24 收藏进度、3 张累计次数，以及展柜中的渔夫。此过程使用实际安全存储，没有注入收藏测试数据。

截图检查覆盖未刮、部分刮擦、职业线索、四区域局部、揭晓、详情原画、图鉴与展柜。发现 360×640 短屏职业证整体缩窄的问题后，改为线索与照片并排，并新增涂层宽度回归断言。最终 APK 再次通过完整门禁。重新安装后检查 360×640 明暗职业证、部分线索恢复及继续刮开：线索和照片并排，票面标题、结果文案与主按钮完整可见，无缩成窄条或布局溢出。正常长屏保留上下排列。

## 交付边界

Flutter 页位于 `app/lib/features/scratch/`，通过登录页和大厅进入，离线即可体验。图鉴、当前票和展柜使用设备本地安全存储；没有云同步、好友排名或 Flutter 展示图片导出。美术为已生成的 24 猫图集裁切；尚未制作独立大尺寸原画、独立徽章和刮擦音效。模拟器验收不代表真实手机触感或性能基准。

## 稀有度主题色强化（2026-09-07）

范围：沿用猫猫刮奖 Core Contract，仅强化奖品视觉识别。

| 需求 | 实现与自动化 | 复核 |
| --- | --- | --- |
| 四档固定主题色，一眼辨认 | 票面彩色横条、图标、边框、底纹与猫名；四档 Widget 颜色绑定测试 | 通过 |
| 未揭晓不能泄露稀有度 | 颜色由 claimed 控制；职业证只完成线索时无横条 | 通过 |
| 下一张重置视觉 | 新票移除稀有度横条；四档重置测试 | 通过 |
| 图鉴、展柜、详情统一 | ScratchRarityLabel 共用标签；收藏卡采用同色细框 | 通过 |

源码复核未发现阻塞 MUST 项；无新增 SHOULD 偏离。稀有度图标作为可选辅助标识，公共导航和按钮保持 Gamebox 语义。

目标 Android 截图检查使用生产 ScratchPage 与 ScratchController，并在单独临时文件存档中加速运行真实随机抽取以获得四档样本；不改玩家存档、不修改正式概率、不绘制替代界面。已在 API 36 Android 运行检查四档结果：常规手机深色模式和 360×640 浅色模式共八个结果状态，以及原画详情、图鉴（含未拥有格）和展柜。颜色绑定一致，标签可读，无票面或操作按钮溢出。传说样本来自隔离检查存档的第 40 次真实随机揭晓；该样本不是概率统计证据。

验证：刮奖专项 16 个测试通过；`bash tool/verify.sh` 通过（7 个顶层检查，17 条汇总警告）。交付 APK 来自正常生产入口构建，已确认不包含临时检查入口。结论：complete。

## 2026-09-07：传统刮奖、未中奖与收藏分组

### 范围与 Profile

Core Contract。用户要求删除多刮法，仅保留传统刮奖；降低获得藏品的概率并加入未中奖；游戏名称中立，分组仅用于收藏浏览。此前三模式验收属于历史版本。

### MUST / SHOULD / MAY

MUST：统一刮奖、不提供分组选择、可揭晓未中奖且继续、中奖才入册、收藏分组独立于游戏名称、兼容旧收藏。代码复核未发现阻塞项。
SHOULD：保留明暗主题、手机安全区、保存中/失败重试、返回与连续刮擦行为，无新增偏离。
MAY：中立暂名采用「刮刮收藏」，中奖率暂设 20%，中奖后四档比例不变。

### 需求追踪复核（运行验收前）

| 需求或状态 | 实现证据 | 自动化证据 | 复核 |
| --- | --- | --- | --- |
| 单一整面刮奖，移除旧控件与顺序逻辑 | page 单一 ScratchSurface；controller 删除 ScratchMode/changeMode；surface 删除线索与爪印绘制 | 四种手机明暗布局断言旧标签不存在；真实 pointer 刮擦 | 通过 |
| 20% 中奖，未中奖独立保存且不入册 | controller winning 持久化；open 仅中奖增加 counts；next 重抽 | .19999/.2/.999 边界、重启、重复揭晓 | 通过 |
| 中奖/未中奖/再来一张 | page 条件绑定横条、边框、文案、详情按钮 | 四档配色与清除；未中奖无详情、无横条且能继续 | 通过 |
| 中立游戏名称，分组只在浏览 | entry/play 中立文案；ScratchGroup/groupId；图鉴分组过滤与分组进度、详情组名 | 手机组件测试断言刮奖页无分组，图鉴显示猫猫百业及进度 | 通过 |
| 旧收藏与当前票保留 | legacy mode 仅用于读取校验与归一化，旧中奖字段缺失默认为 true | 三模式 × 已领/未领共六个迁移用例，校验奖品、次数、日期、展柜及再次加载 | 通过 |
| 保存等待、失败、损坏存档、导航 | 沿用串行写入、禁重复生成、重试与返回守卫 | 已有延迟写入/失败重试/损坏保存/页签切换测试 | 通过 |

当前只有猫猫百业一组。通用藏品类型、groupId 和美术索引已拆出；未来添加内容仍需扩展存档布局与美术，不声称已验证不存在的第二组。

### 测试与目标运行证据

- 聚焦刮奖测试 25 项通过；`dart analyze lib/features/scratch test/features/scratch` 无问题。
- `bash tool/verify.sh`：`PASS verify (7 checks, 17 warnings)`，包含完整确定性测试与实际 Android APK 构建。
- 独占 Android 租约下安装正常生产入口 APK，使用真实安全随机源、真实安全存储和真实触摸输入，无预览入口或固定中奖替身。
- Android API 36，默认大屏深色与 360×640 浅色检查。已捕获并亲自查看旧中奖票恢复、完整涂层、手动部分擦除、手动完成中奖、未中奖、连续下一张、图鉴分组/稀有度组合筛选、详情组名和规则页。
- 原存档 NO.005 普通木匠和收藏 4/24 保留；NO.006 手动刮出稀有魔术师后进度为 5/24；NO.007 未中奖后仍为 5/24。强制停止并重新进入恢复同一 NO.007 未中奖结果，之后可继续 NO.008。
- 大小屏票面、中奖主题色、未中奖中性状态和主要操作清楚可见，图鉴可滚动浏览，分组只在浏览页面出现。无布局溢出或阻塞 UX 问题。
- 临时截图与日志位于忽略的 `artifacts/scratch-flutter/traditional-*`，不纳入固定 gate 或提交。检查后恢复设备尺寸、密度和原深色模式。

### Verdict

complete。

## 2026-09-07：移除规则/展柜，增加玩家收藏

### 范围与 Profile

Core Contract。用户要求删除刮奖规则与我的展柜，加入查看其他玩家收藏。使用现有账号认证和独立收藏 HTTP 服务，保留游客离线刮奖。

### MUST / SHOULD / MAY

MUST：旧规则/展柜入口不可达，玩家列表与他人收藏为真实共享数据，认证绑定写入自己的公开快照；加载/失败/重试/空数据明确，返回不丢失刮奖。代码与聚焦测试复核无阻塞项。
SHOULD：共用 tokens 与手机明暗布局，无新增偏离。
MAY：采取主动公开快照、允许取消公开；仅浏览而非排名，暂不引入好友关系或聊天。

### 运行前需求追踪复核

| 需求/状态 | 实现 | 自动化证据 | 复核 |
| --- | --- | --- | --- |
| 移除规则、展柜与详情收藏到展柜按钮 | ScratchPage 删除 rules/showcase，第三项变玩家 | 页面测试断言旧入口不存在 | 通过 |
| 其他玩家真实列表/详情 | ScratchSocialApi + ScratchPlayersPage，App/登录页/大厅传递认证依赖 | HTTP DTO 验证、玩家跳转与分组/藏品明细测试 | 通过 |
| 加载/错误/重试/分页/游客 | 列表 pending/empty/error，分页 cursor，游客只读 | 延迟请求、错误重试、游客无公开按钮测试 | 通过 |
| 公开/取消公开/账号隔离 | SQLite 迁移005，认证 userId 决定目标，完整快照替换 | 两账号发布、非法输入、匿名拒绝、取消公开不影响另一人、停用过滤 | 通过 |
| 离线刮奖与旧存档 | ScratchController 不依赖网络，旧 favorites 只为存档兼容 | 原中奖/未中奖/迁移/连续手势测试保留 | 通过 |
| 手机明暗可用性 | shared tokens，滚动玩家列表和只读图鉴 | 360×640 明暗玩家页测试 | 通过 |

运行证据待本节后续补齐；没有线上部署，不把本地测试账号描述为真实线上玩家。

### 目标运行与 UX 检查结果

- Flutter 聚焦测试 29 项通过；最终完整门禁 `PASS verify (7 checks, 17 warnings)`。Go HTTP 测试另覆盖 32 人跨页、游标非法参数及负数/超限数量。
- 使用当前 worktree 的 `gameboxd`、隔离 SQLite 与 API `127.0.0.1:27182`；Android 正常生产入口 APK 通过 `GAMEBOX_API_BASE_URL=http://10.0.2.2:27182` 连接真实服务。未使用静态 UI、假玩家客户端分支或线上服务。
- 两个独立账号「测试玩家甲/乙」经真实注册和公开接口创建；其藏品数量为明确构造的验收样本，不声称来自真实玩家或随机抽取。游客 Android 能看到两人各 3/24，进入只读图鉴核对藏品、分组、稀有度和重复数量。
- Android 通过原生注册页创建第三个本地测试账号 SocialTester，公开已有实际本地收藏。独立 HTTP 客户端验证该快照为 5 种、共 6 件；Android 取消公开后独立客户端验证只移除该账号，保留甲/乙。
- 停止本地服务后，Android 显示网络错误与重试；服务重启后快照仍在，点重试恢复列表。临时撤下两条测试快照验证真实空列表，再恢复两条示例。公开/取消公开不改变本地刮奖存档。
- API 36 模拟器，大屏深色与 360×640 浅色：截图检查刮奖页、游客/已登录玩家列表、公开/取消公开、他人图鉴、网络失败、恢复和空列表。规则与展柜入口不可达，主要操作/文字无溢出。
- 初次发现他人图鉴原画因非正方形约束露出下一行图集边缘；改为居中的正方形约束，加入尺寸回归测试，并重建 APK 在明暗两尺寸复查通过。
- 图片/日志/测试账号凭据留在忽略的 `artifacts/scratch-flutter/`，凭据文件仅用户可读，不打印到日志或截图。固定 gate 未新增截图逻辑。
- 设备恢复原尺寸、密度及深色模式。最终本地测试 APK 与安装版本均连接 worktree 后端；未执行生产部署。

### 最终 Verdict

complete（本地实现与真实客户端/后端验收；线上服务尚未部署）。

## 2026-09-07：玩家列表去掉解释文本

范围沿用 Core Contract。按用户反馈，删除重复页头、公开收藏介绍、发布说明和游客登录说明。保留玩家昵称、收集进度、更新时间、公开/取消公开、刷新及必要的加载、成功和失败状态。

需求复核：`ScratchPlayersPage.build` 的登录和游客分支均已删除常驻解释文本；无新增帮助入口、替代说明卡或交互改动。现有 social widget tests 覆盖游客浏览、发布/取消、加载失败重试和详情导航；纯文案删除不新增镜像测试。MUST 无代码阻塞项，SHOULD 无新增偏离，MAY 无新增功能。

验证：`verify_fast.sh` 16 项检查通过，正常入口 Android debug APK 构建成功。安装到 API 36 模拟器，检查深色常规屏及浅色 360×640 玩家列表截图：说明文字已移除，操作与两条测试玩家记录清晰、无裁切或溢出。结论：complete。

## 2026-09-07：默认公开和自动同步

范围沿用 Core Contract。MUST：移除公开/更新/取消按钮和 DELETE 能力，全部启用账号默认可见，本地收藏持久化成功后自动同步。SHOULD：保留离线刮奖、失败重试、明暗主题；无新增偏离。MAY：保留刷新列表操作。

需求追踪复核：玩家页删除全部发布状态和操作；服务端 users LEFT JOIN 收藏快照使从未发布或旧版取消公开的账号也可见，缺失快照返回零数量；POST 认证和校验保持兼容；DELETE 返回 405。同步器监听本地保存完成，串行传输最新快照、跳过未保存/读取失败、失败退避重试并在退出时清理。对应自动化覆盖加载后上传、新中奖、相同内容去重、请求中变化、断网恢复、游客/坏存档、全账号分页与拒绝隐藏。复核未发现阻塞项。列表没有新增解释段落。

验证：修复测试 fake 的继承声明后，`verify_fast.sh` 16 项通过；正常入口 Android debug APK 构建成功。真实本地后端中，原本没有公开快照的 SocialTester 在进入游戏前已显示零收藏；Android 进入游戏自动同步后，独立匿名 HTTP 客户端读到 5 种、6 件收藏，无发布操作。检查 API 36 Android 常规深色和 360×640 浅色列表截图，公开/更新/取消按钮与说明文字均不存在，三名测试玩家及进度可读，无溢出。断网退避及上传期间新收藏由同步器测试覆盖，本轮未重复设备断网实验。未部署生产。结论：complete。

## 2026-09-07：C 原画收藏卡＋B 数量角标

范围：Core Contract，用户批准 C 原画收藏卡，并指定结合 B 右上角数量标签。自己的图鉴和他人收藏共用 `ScratchCollectibleCard`：正方形原画铺满卡宽、底部稀有度色块显示名称与等级、右上角贴边主题色数量角标，未获得项保持中立灰色。移除原先圆形徽章、小编号和分散的标签。

需求追踪复核：四档主题色复用现有 token；图片保持 square atlas 裁切，不暴露相邻素材；自己的卡片保留既有 key 和详情点击，其他玩家保持只读；数量来自真实持有数。四档明暗组件测试覆盖色块、全宽正方形画面、数量、点击和未获得隐藏美术。MUST 无代码阻塞项；SHOULD 无新增偏离；MAY 为灰色锁形占位，不增加新美术依赖。

首轮 Android 检查发现自己的图鉴沿用三列，未体现批准稿的大原画；已统一两列，将卡片宽高比从 .66 调整为 .74，原画约占卡高 74%，缩减底栏留白。数量角标贴合右上边缘，内侧仅左下角圆角。

最终验证：`verify_fast.sh` 16 项通过，新增卡片组件明暗测试通过；正常生产入口 APK 重建安装成功。检查最终两列自有图鉴（含未获得）和他人收藏深色，以及 360×640 浅色他人收藏：原画无相邻图集泄露，数量角标不遮挡脸部，底栏名称/等级清晰，无布局溢出；长列表正常滚动。结论：complete。

## 2026-09-07：卡片详情持有者

范围 Core Contract。MUST：从自有图鉴、未获得预览或刮中奖品进入卡片详情，查看持有同一目录索引卡片的玩家昵称和数量。服务端先按 card 筛选再分页，排除零持有和停用账号；点击玩家打开其只读收藏。SHOULD 无新增偏离，MAY 保留分页按钮。

需求追踪复核：`ScratchCardOwners` 接入现有详情滚动区域，不依赖已加载的全玩家列表；打开时先尝试同步本地最新收藏。组件测试覆盖持有数、查询索引、加载、错误重试、空状态、分页与玩家导航；HTTP 测试覆盖筛选、准确数量、空结果、非法索引和停用。代码复核无阻塞项。

最终证据：verify-fast 16 项通过、Flutter 刮奖专项 36 个测试通过、Go 收藏接口测试通过（含 32 名持有者跨页）。真实本地 HTTP 与 Android 魔术师详情一致显示两名持有者各 ×1；点击测试玩家甲进入其收藏，返回回到原卡片详情。检查深色详情与 360×640 浅色滚动后的列表截图，无溢出。启动时模拟器曾出现 startup ANR（CPU 压力日志），选择等待后正常恢复，本轮未改动启动逻辑。构建为正式入口 debug APK；未部署生产。结论：complete。
