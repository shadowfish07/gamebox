# 卡片详情收藏概览验收

2026-09-08。范围为 Flutter 收藏详情，采用 Core Contract。

## 需求追踪复核

| 批准要求与状态 | 实现 | 自动化证据 |
| --- | --- | --- |
| 突出个人张数、首次相遇、未收藏 | `ScratchCardDetail._ownership`，监听 controller | 明暗主题、十亿张窄屏、未收藏和抽卡入口 |
| 独立查看入口、点击才加载 | `_ownersOpened` 与 `scratch-view-owners` | 默认零请求，重复进入不重载 |
| 持有者视图保留卡片上下文 | 缩略图、名字、稀有度；按 card 查询 | 查询索引与准确张数 |
| 玩家收藏、列表、故事之间返回保留位置 | IndexedStack、ScrollController、PageStorageKey、PopScope | 玩家页返回、系统返回、可见返回与位置断言 |
| 加载、失败重试、空记录、分页 | `ScratchCardOwners` 保留原数据状态机 | pending、重试恢复、空记录、分页查询 |
| 移除旧日期行和默认展开列表 | `ScratchPage._detail` 接入独立组件 | 默认无列表查询，旧文本已移除 |

代码复核与运行时迭代发现并修复了固定缩略图尺寸、窄屏首屏数量被原画挤出、极大数量横向溢出。空状态改为“暂无持有玩家记录”，避免把未同步的本地收藏解释成无人获得。

MUST 无未解决阻塞；SHOULD 无新增偏离；MAY 使用固定比例面板，首次打开后保留列表直到关闭详情。不改奖池、抽卡、同步协议或玩家排序。

## 测试与运行时

- 聚焦 `scratch_card_detail_test.dart`、`scratch_card_owners_test.dart`：5 项通过。
- 最终 `bash tool/verify.sh`：PASS，7 checks、17 warnings，包含锁定依赖、分析、Flutter 测试、设计系统、Go/Godot 检查、Android 单测与 debug APK 构建/断言。警告包含锁定依赖可升级提示和现有代码风格提示。
- 在 slot 1 租约下使用 API 36 `emulator-5564` 安装最终正式入口 debug APK；请求通过本地转发连接工作树真实 Go 服务及隔离 SQLite，没有使用假 UI。
- 实际抽卡得到农夫，检查已拥有概览；图鉴预览魔术师，检查未收藏概览及两名真实测试账号的持有行。点击玩家打开其收藏，Android 返回回到列表，再返回到原详情位置。主动暂停本地转发进程验证加载、请求超时和重试恢复。
- 亲自检查 360×640 浅色/深色与约 411×891 深色的实际截图：数量和查看入口在窄屏首屏可见，缩略图上下文清楚，无溢出；空、加载、错误、玩家列表布局可用。截图为忽略目录中的临时验收输入，不提交。
- 未部署生产；模拟器结果不代表真机性能或双设备网络验收。本次未改变那些边界。

Verdict: complete。
